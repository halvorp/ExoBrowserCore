# Rust build artifacts
/target/
**/*.rs.bk

# Host C build artifacts
host/*.o
host/*.a
host/gutted-host
host/run*.log

# Test / demo leftovers
/0
/*.ppm
/*.png
/*.log
/cog.log

# Harness / editor state
.claude/
.idea/
.vscode/
*.swp

# Secrets (aider / OpenRouter)
.env
# Aider runtime state (never these — but keep .aider.conf.yml / .aiderignore in the tree)
.aider.chat.history.md
.aider.input.history
.aider.tags.cache.v*/
.aider.llm.history
.aider*
